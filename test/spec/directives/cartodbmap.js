'use strict';

require('../../../app/scripts/app.js');

describe('Directive: cartodbMap', function () {

    // load the directive's module
    beforeEach(angular.mock.module('midjaApp'));

    ////var element,
    ////    scope;
    //
    //beforeEach(inject(function ($rootScope) {
    //    scope = $rootScope.$new();
    //}));
    //
    //it('should make hidden element visible', inject(function ($compile) {
    //    //element = angular.element('<cartodb-map></cartodb-map>');
    //    //element = $compile(element)(scope);
    //    //expect(element.text()).toBe('this is the cartodbMap directive');
    //}));
});
